import Image from "next/image";
import { Bell, CalendarDays, Menu } from "lucide-react";

export function AdminHeader({ onMenu, adminName = "Yönetici" }: { onMenu: () => void; adminName?: string }) {
  const today = new Intl.DateTimeFormat("tr-TR", { weekday: "long", day: "numeric", month: "long", year: "numeric" }).format(new Date());
  return <header className="flex min-h-24 items-center gap-4 border-b border-slate-200 bg-white px-5 md:px-8"><button onClick={onMenu} className="grid size-10 shrink-0 place-items-center rounded-lg border border-slate-200 text-slate-600 lg:hidden" aria-label="Menüyü aç"><Menu size={20}/></button><div className="relative h-[68px] w-[204px] shrink-0 sm:h-[72px] sm:w-[216px]"><Image src="/images/logo-sehitler.png" alt="Abidos Pansiyon Çanakkale" fill priority sizes="216px" className="object-contain object-left"/></div><div className="ml-auto flex items-center gap-3"><span className="hidden items-center gap-2 text-xs capitalize text-slate-500 xl:flex"><CalendarDays size={16}/>{today}</span><button className="relative grid size-10 place-items-center rounded-full bg-slate-100 text-slate-500" aria-label="Bildirimler"><Bell size={18}/><i className="absolute right-2 top-2 size-1.5 rounded-full bg-red-500"/></button><div className="hidden border-l border-slate-200 pl-4 md:block"><b className="block text-xs text-slate-700">{adminName}</b><small className="text-[10px] text-slate-400">Yönetici</small></div></div></header>;
}
